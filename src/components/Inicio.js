// src/components/Inicio
import React from 'react'; // Importa React si aún no lo has hecho
import Body from './layout/Body'; // Asegúrate de ajustar la ruta correcta

const Inicio = () => {
    return (
        <div>

            <Body /> {/* Incrusta el componente Body aquí */}
        </div>
    );
};

export default Inicio;